function func1() => void
{
  local a: int;
}

function main() => void  
{
  local x: int;                                    // 1.1
  local y: int;                                    // 1.1
  local z: int;
  x := 2;
  y := 3;
  z := x*y/2;
  write(x);
  write(y);                                   // 3.4 
  write(z);
}