class CALCULATOR {
  private attribute memory: float;
  public attribute x: int[10];
  public attribute y: float[10];
};

function func1() => void
{
  local a: int[10][34];
  local calcs: CALCULATOR[10];
}

function main() => void  
{
  local x: int;                                    
  local y: int;                                    
  local z: int;
  x := 3;
  y := 2;
  z := x * y;
  if (x <> y+1) 
        then {
            write (x-y);
        } else {
            write (y);
            write (x);
        };
  local i: int;
  i := 0;
  while (i<=10){
    write(i);
    i := i + 1;
  };
  write(z);
}