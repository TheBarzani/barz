function main() => void  
{
  local a: int;
  local x: int;                                    // 1.1
  local y: int;                                    // 1.1
  local z: int;
  x := 1;
  y := 2;
  z := 3;
  y := 5 + 4 * z;                                   // 3.1, 5.1
  read(x);                                         // 3.4
  a := 9;
  write(x+y+a);
}