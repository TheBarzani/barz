class CALCULATOR {
  private attribute memory: float;
  public attribute x: int[10];
  public attribute y: float[10];
};

function func1() => int
{
  local a: int;
  a := 111; 
  return(a);
}

function main() => void  
{
  local x: int;                                    
  local y: int;                                    
  local z: int;
  local ret: int;
  ret := func1();
  write(ret);
  x := 3;
  y := 2;
  z := x or 0;
  if (x < y) 
        then {
            // write (x-y);
        } else {
            // write (y);
            // write (x);
        };
  local i: int;
  i := 0;
  while (i<=10){
    // write(i);
    i := i + 1;
  };
  // write(z);
}